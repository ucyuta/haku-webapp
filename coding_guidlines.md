# Haku: WebApp (working title)

(ほぼ自分用)

## ファイル名

1. Viewはローワーキャメルケースを使用する（例：appDocuments.tsx）

2. Controller、UseCase、Repository、Entityなどはアッパーキャメルケースを使用する（例：LoginController.ts）

3. MarkDownファイルはスネークケースを使用する（例：conding_guidlines.md）

## 関数・変数・クラス名

1. 変数名はローワーキャメルケースを使用する（例：const fetchData）

2. 関数名はローワーキャメルケースを使用する（例：function getData）

3. クラス名はアッパーキャメルケースを使用する（例：class UserEntity）

4. JSX.Elementを返す関数に関してはアッパーキャメルケースを使用する（例：function LoginPage）

## スタイリング

1. タブは半角スペース2文字分とする

2. コードの最後には必ず「;」をつける

3. 文字列は基本的にシングルクォーテーションを使用する
